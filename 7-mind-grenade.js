const num1 = 5;
const num2 = 10;

function addValues(){
    console.log(`the sum is: ${num1 + num2}`)
}

addValues()

//if we have a module that invokes variable then inspite of not assigning that module to a variable it runs!